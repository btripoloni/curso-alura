    <h4><%= t ".said", name: comment.name %></h4>
    <%= simple_format comment.body %>
    Sent <%= t ".send", timestamp: time_ago_in_words(comment.created_at) %> ago
    <%= link_to t('link.destroy'), comment_path(comment), method: :delete, confirm: t('link.destroy_confirmation')  %>